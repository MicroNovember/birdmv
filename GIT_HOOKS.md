# Git Hooks - คุณภาพระหรับความคุณภาพ

SAMORN Movie ใช้งาน Git Hooks ในการป้องกันการ commit ที่อาจทำให้เกิดข้อผิดพลาด และรักษาคุณภาพของโปรเจค

## 📋 ที่ติดตั้งไว้

### 🔒 Pre-commit Hook
- **ตรวจสอบก่นการ commit** ทุกครั้ง
- **ป้องกันการ commit ที่มีปัญหา:**
  - `console.log` statements (ควรใช้ `console.error` หรือลบทิ้ง)
  - `alert()` statements (ควรใช้ `Alert2`)
  - Merge conflict markers (`<<<<<<<`, `=======`, `>>>>>>>`)
  - JavaScript syntax errors
  - HTML syntax errors
  - ข้อมูล sensitive (API keys, secrets)

### 🚀 Pre-push Hook  
- **ตรวจสอบก่อการ push** ไปยัง remote repository
- **ตรวจสอบพิเศษเมื่อ push ไป main/master branch:**
  - ไม่มี uncommitted changes
  - ไม่มี sensitive files ที่ถูก tracking
  - ไม่มีไฟล์ขนาดใหญ่ (>5MB)
  - ไม่มี secrets ใน commits ล่าสุด
- **ตรวจสอบ commit message format**

### 📝 Commit-msg Hook
- **ตรวจสอบข้อความ commit message:**
  - ความมีความอย่างน้อย 10 ตัวอักษร
  - แนะนำให้ใช้ภาษาไทย (ถ้าเป็นไทย)
  - ไม่มี trailing whitespace
  - ไม่ลงท้ายด้วย period
  - ไม่เริ่มตัวพิมพิเศษ (WIP, FIXME, TODO, HACK, XXX)

## 🛠️ การติดตั้ง

### วิธีที่ 1: ใช้ Setup Script (แนะนำ)
```bash
# รัน setup script
./setup-hooks.sh
```

### วิธีที่ 2: ติดตั้งแบบ Manual
```bash
# ทำให้ hooks executable
chmod +x .git/hooks/pre-commit
chmod +x .git/hooks/pre-push
chmod +x .git/hooks/commit-msg

# หรือติดตั้งจาก template
cp .git/hooks/pre-commit.sample .git/hooks/pre-commit
cp .git/hooks/pre-push.sample .git/hooks/pre-push
cp .git/hooks/commit-msg.sample .git/hooks/commit-msg
```

## 🔧 การใช้งาน

### ปกติดตั้งแล้ว
เมื่อคุณ `git commit` หรือ `git push` hooks จะทำงานอัตโนมัติ:

```bash
$ git commit -m "เพิ่มฟีเจอร์ VIP"
🔍 Running pre-commit checks...
📋 Checking for common issues...
✅ Pre-commit checks passed!
🚀 Ready to commit your changes!
[master abc1234] เพิ่มฟีเจอร์ VIP
 1 file changed, 25 insertions(+)
```

### ถ้าพบปัญหา
```bash
$ git commit -m "fix bug"
🔍 Running pre-commit checks...
📋 Checking for common issues...
❌ ERROR: Found console.log statements in staged files
   Please remove console.log statements before committing
   Use console.error() for errors or remove entirely
```

## 🎯 กฎเกณะการที่ตรวจสอบ

### ✅ ผ่านผ่าน
- ✅ ไม่มี `console.log` ใน code
- ✅ ใช้ `Alert2` แทน `alert()`
- ✅ ไม่มี merge conflicts
- ✅ ไม่มี sensitive data ใน code
- ✅ JavaScript และ HTML syntax ถูกต้อง
- ✅ Commit message มีความที่เหมาะสมควร

### ⚠️ แจ้งเตือน
- ⚠️ มี `console.log` หรือ `alert()` แต่จะแนะนำให้แก้ไข
- ⚠️ มี merge conflicts ต้องแก้ไขก่อน commit
- ⚠️ มีไฟล์ขนาดเกิน 5MB
- ⚠️ Commit message สั้นเกินไปหรือยาวน

### ❌ บล็อกการ
- ❌ มี console.log หรือ alert() ใน staged files
- ❌ มี merge conflicts
- ❌ มี sensitive data ที่ถูก tracking
- ❌ JavaScript หรือ HTML syntax errors
- ❌ Commit message สั้นเกิน 10 ตัวอักษร

## 🔄 การปิดใช้งาน Hooks

### ชั่วคราว (ถ้าต้องการ)
```bash
# ข้าม commit โดยไม่ตรวจสอบ
git commit --no-verify -m "Emergency fix"

# ข้าม push โดยไม่ตรวจสอบ  
git push --no-verify origin master
```

### ถายกต้องการแก้ไข
```bash
# แก้ไข hooks ชั่วคราว
nano .git/hooks/pre-commit

# หรือใช้ setup script ใหม่
./setup-hooks.sh
```

## 📝 การปรับแต่ง Hooks

คุณสามารถายแก้ไข hooks ได้ตามความต้องการ:

1. **แก้ไฟล์ hook**:
   ```bash
   nano .git/hooks/pre-commit
   ```

2. **แก้ไข้อความตรวจสอบ**:
   ```bash
   # เพิ่มการตรวจสอบใหม่ pre-commit hook
   echo "echo 'Checking for custom rules...'" >> .git/hooks/pre-commit
   ```

3. **รัน setup script ใหม่**:
   ```bash
   ./setup-hooks.sh
   ```

## 🎯 ประโยช์

Git hooks ช่วยให้:
- ✅ **ป้องกันข้อผิดพลาด** ก่อนเข้าสู่ repository
- ✅ **รักษาคุณภาพ** ของโครเจค
- ✅ **บังคุณภาพ** ที่สม่วนและเข้าใจ
- ✅ **ลดเวลา code** และ documentation
- ✅ **ปฏิบัติภาพ** ที่สอดคลอน

**🚀 ตอนนี้ commit ของคุณจะมีคุณภาพที่ดีขึ้น!**
